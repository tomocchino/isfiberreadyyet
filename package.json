{
  "name": "isfiberreadyyet",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.8.0"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "victory": "^0.13.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "react-scripts build",
    "deploy": "rsync -avze ssh --delete --exclude '*.map' build/ bluehost:public_html/isfiberreadyyet/"
  }
}
